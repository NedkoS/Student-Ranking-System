@model StudentRanking.Models.ProgrammeRules

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ProgrammeRules</legend>

        @Html.HiddenFor(model => model.ProgrammeName)

        <div class="editor-label">
            @Html.LabelFor(model => model.MaleCount)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MaleCount)
            @Html.ValidationMessageFor(model => model.MaleCount)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.FemaleCount)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FemaleCount)
            @Html.ValidationMessageFor(model => model.FemaleCount)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
