@model StudentRanking.Models.Exam

@{
    ViewBag.Title = "Create";
    //float fGrade = 2;// float.Parse(Model.Grade.ToString());
    //double dGrade = Model.Grade;
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Exam</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.StudentEGN)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StudentEGN)
            @Html.ValidationMessageFor(model => model.StudentEGN)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ExamName)
        </div>
        <div class="editor-field">

            @*@Html.EditorFor(model => model.ExamName)
            @Html.ValidationMessageFor(model => model.ExamName)*@

            @Html.DropDownListFor(model => model.ExamName, ViewData["currentExamName"] as SelectList)
        </div>
        <br>
        <div class="editor-label">
            @Html.LabelFor(model => model.Grade)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Grade)
            @Html.ValidationMessageFor(model => model.Grade)
        </div>


        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
